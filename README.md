# g2_projettutore_p2_2526

Système de gestion d'équipements

## How to install

Une fois avoir cloné le projet, faites :
python -m venv env
source env/bin/activate
pip install -r requirements.txt (Et hop, ils ont tout ton environnement !)

#installation des dependances :
~ utiliser la commande! ~

- pip install -r requirements.txt
